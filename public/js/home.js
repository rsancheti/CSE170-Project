$(document).ready(function() {
	initializePage();
})

function intializePage() {
  var data = require('../data.json');
  var dataSize = data.length;
  // loop through data size
  // .append(title) to dropdown list
}
